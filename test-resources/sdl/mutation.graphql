directive @auth(
  role: Role
) on FIELD_DEFINITION

enum Role {
  ADMIN
  USER
  GUEST
}

type Mutation {
  """
  로그인된 사용자의 직업군을 설정
  """
  setOccupation(
    """
    직업군의 ID
    """
    id: ID!
  ): SetOccupationPayload! @deprecated(reason: "deprecated") @auth(role: ADMIN)
}
